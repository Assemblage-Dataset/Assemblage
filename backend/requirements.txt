# unused dependencies have been commented out: if I can't figure out why they were installed, they're commented without a note
alembic==1.16.5  # required for database version management
alembic-postgresql-enum==1.8.0  # required by alembic
annotated-types==0.7.0  # required by alembic
Mako==1.3.10 # required by alembic
MarkupSafe==3.0.2  # required by alembic
typing_extensions==4.15.0  # alembic
boto3==1.40.37  # required for S3/MinIO/AWS objects
botocore==1.40.37  # required for S3/MinIO/AWS objects
jmespath==1.0.1  # required for S3/MinIO/AWS objects
# cachetools==5.5.2   # required by Google requirements
#certifi==2021.10.8  # used by Google and FastAPI
#charset-normalizer==2.0.7
clang==20.1.5  # used by clang builder
#click==8.3.0   # used by CLI
#cycler==0.10.0
#Deprecated==1.2.13
# anyio==4.11.0  # required for website only  - not currently used
#fastapi==0.117.1  # required for website only  - not currently used
#fastapi-cli==0.0.13  # required for website only  - not currently used
#fastapi-cloud-cli==0.2.0  # required for website only  - not currently used
#h11==0.16.0  # required by FastAPI
#httpcore==1.0.9  # required by FastAPI
# Pygments==2.19.2  # FastAPI
#mdurl==0.1.2  #fastapi
#httpx==0.28.1  # used by fastapi
GitPython==3.1.27  # used by vcpkg 
gitdb==4.0.12   # used by gitpython
smmap==5.0.2  # used by gitpython
# google-api-core==2.25.1
# google-api-python-client==2.181.0
# google-auth==2.40.3
# google-auth-httplib2==0.2.0
# google-auth-oauthlib==1.2.2
# googleapis-common-protos==1.70.0
#httplib2==0.31.0  # used by googleapi
idna==3.10  # used by email validator? figure this one out
# markdown-it-py==4.0.0  # unused by any of the main stuff -- personal preference i suppose if you want to use cmdline tools
#oauthlib==3.3.1 # for google stuff
packaging==25.0  # required by pipdeptree
pefile==2021.9.3  # used by builder and loader
future==0.18.2  # required by pefile
pika==1.2.0  # required for rabbitmq
pipdeptree==2.28.0  # useful for dependency management
# proto-plus==1.26.1  # only possibly used in the unused cloner_new worker
# protobuf==6.31.1  # only possibly used in the unused cloner_new worker
psycopg2-binary==2.9.10  # used by DB/SQL
#pyaml==25.7.0
# pyasn1==0.6.1  # google thing
# pyasn1_modules==0.4.2   # google thing
pydantic==2.11.9  # used for config
pydantic-settings==2.11.0  # used for config
pydantic_core==2.33.2  # used for config
pyelftools==0.27  # probably used by analyze and builder
#pyftpdlib==1.5.6  # deprecated, from ftp server
# PyJWT==2.3.0 
#pyparsing==3.2.3
#python-dateutil==2.8.2
python-dotenv==1.1.1  # allegedly used in config.py
requests==2.32.5
#requests-oauthlib==2.0.0  # google
# rich==14.1.0
# rich-toolkit==0.15.1
# rignore==0.6.4
# rsa==4.9.1  # google
s3transfer==0.14.0  # boto requirement
#sentry-sdk==2.38.0 # fastapi
setuptools==80.9.0  # i assume it's needed somewhere, don't know where
# shellingham==1.5.4 # fastapi
six==1.16.0  # needed by boto
#sniffio==1.3.1 # watchfiles dep
greenlet==3.2.4  # required by SQLAlchemy
SQLAlchemy==2.0.43
SQLAlchemy-Utils==0.38.2
sqlmodel==0.0.25
#starlette==0.48.0  # fastapi dep
tqdm==4.62.3  # not used by core workers
#typer==0.19.2  # fastapi
#typing-inspection==0.4.1 # fastapi
# uritemplate==4.2.0  # google
urllib3==2.5.0  # boto
#uvicorn==0.37.0  # fastapi
#uvloop==0.21.0; sys_platform != "win32"   # required for website only (FastAPI) - not currently used
#watchfiles==1.1.0
#websockets==15.0.1
wheel==0.45.1
#wrapt==1.17.3
