# Legacy build modules for DeepHistory dataset reconstruction.
# These are Conan-based build strategies that were originally used to build
# the DeepHistory binary corpus (multi-version Windows binaries with PDB info).
# Kept separate from the main Assemblage pipeline to avoid interference.
